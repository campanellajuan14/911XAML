<UserControl x:Class="NineOneOneReality.Launcher.Views.Inactive.InactiveScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Left"
             VerticalAlignment="Top"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">

    <!--
        Full client PNG at native aspect ratio (no crop). Viewbox scales down only if
        the image is larger than the monitor work area; window uses SizeToContent.
    -->
    <Viewbox x:Name="ArtHost"
             Stretch="Uniform"
             HorizontalAlignment="Left"
             VerticalAlignment="Top">
        <Image x:Name="ArtImage"
               RenderOptions.BitmapScalingMode="NearestNeighbor"
               RenderOptions.EdgeMode="Aliased"
               SnapsToDevicePixels="True"
               Stretch="None" />
    </Viewbox>
</UserControl>
